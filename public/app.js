document.addEventListener('DOMContentLoaded', () => {
	const app = document.getElementById('app');
	app.innerHTML = `
		<div class="card">
			<p>Server is running. Frontend placeholder is ready.</p>
			<p class="muted">Next: implement projects grid consuming /api.</p>
		</div>
	`;
});


